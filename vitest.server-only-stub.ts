// Stub for `import "server-only"` in unit tests. The real module throws
// when imported from a Client Component build; here we just no-op so we
// can unit-test server-side data helpers in jsdom.
export {};
